html, body {
    margin 0
    padding 0
    color rgba(0, 0, 0, 0.87)
    position relative
    min-height 100%
}

*, *:before, *:after
    box-sizing border-box
    user-select none
    -webkit-tap-highlight-color rgba(0,0,0,0)


.raml-doc {
    display flex
    min-height 100%
}
